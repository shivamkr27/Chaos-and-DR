# Apply after region-primary

project          = "chaos-dr"
aws_region       = "us-west-2"
db_password      = "CHANGE_ME_same_as_primary"
api_key          = "CHANGE_ME_same_as_primary"
ssh_public_key   = "ssh-rsa AAAA... same key as primary"
app_image        = "your-dockerhub-username/chaos-dr-app:latest"
allowed_ssh_cidr = "1.2.3.4/32"   # REQUIRED — your IP only, no default (was 0.0.0.0/0)
