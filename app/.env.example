PORT=3000
DB_HOST=localhost
DB_PORT=5432
DB_NAME=chaosdb
DB_USER=postgres
DB_PASSWORD=postgres
# Set to "false" for local Postgres (no SSL). Leave unset/true for AWS RDS.
DB_SSL=false
# Required to allow POST/DELETE on /api/items — generate a random value per environment
API_KEY=change-me
